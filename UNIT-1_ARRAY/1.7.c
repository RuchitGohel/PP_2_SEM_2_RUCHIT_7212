//Merge two arraays
//RUCHIT_GOHEL_7212
//21-02-24

#include<stdio.h>

void main()
{
    int a[30],b[30],i,n,m,j;

    printf("\nEnter number of elements for a:");
    scanf("%d", &n);
    for(i=0;i<n;i++)
    {
        scanf("%d", &a[i]);
    }
    printf("\nElements of array a are:\n", a[i]);

    printf("\nEnter number of elements for b:");
    scanf("%d", &m);
    for(j=0;j<m;j++)
    {
        scanf("%d", &b[j]);
    }
    printf("\nElements of array a are:\n", b[j]);

    for(i=0;i<n;i++)

}
